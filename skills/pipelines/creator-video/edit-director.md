# Creator Video Edit Director

Produce `edit_decisions` from `scene_plan` and `asset_manifest`.

Carry forward `render_runtime` and `composition_mode` from the approved proposal. Build cuts around the strongest user assets first, then Seedance clips, then supporting cards or stock. Use vertical pacing by default for short-form output unless the proposal says otherwise.
